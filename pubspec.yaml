name: daku
description: A tinder for Products

publish_to: 'none'
version: 1.0.0+5

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  youtube_plyr_iframe: ^2.0.0
  graphql_flutter:
  rive: ^0.6.8
  url_strategy: ^0.2.0
  google_fonts:
  url_launcher:
  swipable_stack: 0.3.0
  hive:
  provider:
  cached_network_image: ^2.5.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  rename:
  flutter_launcher_icons: "^0.8.0"

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/icon/icon.png"
  adaptive_icon_background: "#ffffff"

flutter:

  uses-material-design: true
  assets:
    - assets/images/
    - assets/riv/
